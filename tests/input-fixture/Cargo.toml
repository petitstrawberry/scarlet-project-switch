[package]
name = "scarlet-input-qa-fixture"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
scarlet = { path = "../../../Scarlet/kernel", default-features = false, features = ["network"] }
